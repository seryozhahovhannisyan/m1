/* Compiled with love by Gruntfile.js */
var {{msgKeys.filename}} = [
{{#each msgKeys.keys}} {{this.name}} = {  {{#each this.value}}  "{{this.key}}": {{#if this.value}}{{{this.value}}}{{else}}null{{/if}}{{#unless @last}},{{/unless}} {{/each}} }{{#unless @last}},{{/unless}}
{{/each}}
]
var valueOf{{msgKeys.filename}} = function (id) {
    for(var t in {{msgKeys.filename}}){
        if({{msgKeys.filename}}[t].id == id){
            return {{msgKeys.filename}}[t];
        }
    }
    return null;
}